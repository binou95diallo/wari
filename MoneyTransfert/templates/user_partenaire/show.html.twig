{% extends 'base.html.twig' %}

{% block title %}UserPartenaire{% endblock %}

{% block body %}
    <h1>UserPartenaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_partenaire.id }}</td>
            </tr>
            <tr>
                <th>Matricule</th>
                <td>{{ user_partenaire.matricule }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ user_partenaire.username }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ user_partenaire.password }}</td>
            </tr>
            <tr>
                <th>NomComplet</th>
                <td>{{ user_partenaire.nomComplet }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ user_partenaire.adresse }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ user_partenaire.telephone }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user_partenaire.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('user_partenaire_index') }}">back to list</a>

    <a href="{{ path('user_partenaire_edit', {'id': user_partenaire.id}) }}">edit</a>

    {{ include('user_partenaire/_delete_form.html.twig') }}
{% endblock %}
