<<<<<<< HEAD
# TEST
# global backend actions remove all default actions for the 'new' view
# (but the special "save" action will still be displayed because it's mandatory)

# CONFIGURATION
easy_admin:
    new:
        actions:
            ['-list']
    entities:
        Category:
            class: AppTestBundle\Entity\UnitTests\Category
=======
# TEST
# global backend actions remove all default actions for the 'new' view
# (but the special "save" action will still be displayed because it's mandatory)

# CONFIGURATION
easy_admin:
    new:
        actions:
            ['-list']
    entities:
        Category:
            class: AppTestBundle\Entity\UnitTests\Category
>>>>>>> 920aea0ab65ee18c3c6889c75023fc25561a852b
